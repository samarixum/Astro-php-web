<?php
    // Initialize the session
    session_start();
    
    // Unset all of the session variables
    $_SESSION = array();
    
    // Destroy the session.
    session_destroy();
    
    // Redirect to previous page
    echo '<script nonce="inline" type="text/JavaScript">     javascript:history.back();     </script>';
    //header("location: ../index.php");
    exit;
?>